services:
  vite-dev:
    container_name: angular-auth-vite-dev
    build:
      context: .
      dockerfile: Dockerfile-dev
    ports:
      - "9999:9999"
    volumes:
      - .:/app
      - /app/node_modules
    environment:
      - NODE_ENV=development